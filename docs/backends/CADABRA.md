# Cadabra2 backend

Optional. GPL-3 executable, not vendored.

Detect: `cadabra2 --version`

If missing: `OPTIONAL / unavailable`. Core tests still pass.

Intended interchange (when a run provides index declarations):

Input: expression, index types, tensor properties, assumptions.
Output: canonical index form, dummy-index equivalence, symmetry orbit
metadata — all **descriptive** unless a witness is attached.

This environment: backend UNAVAILABLE.
