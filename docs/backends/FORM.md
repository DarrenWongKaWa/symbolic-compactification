# FORM backend

Optional. GPL executable (`form` / `tform`), not vendored.

Detect: `which form`

Use: term streaming / grouping for expressions too large for RAM-bound
SymPy. Performance is not scientific superiority.

This environment: backend UNAVAILABLE. No FORM-vs-SymPy scaling numbers
were collected.
