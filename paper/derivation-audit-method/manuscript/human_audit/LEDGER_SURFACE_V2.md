# Human-audit ledger surface v2

Verdict: **LEDGER_SURFACE_V2** (presentation). Not scientific authority.

Date (UTC): 2026-09-02  
Parent: `PRODUCT_HONESTY_LOCK.md`, `PAPER_AUTHORITY_LOCK.md`  
Canonical: `manuscript/human_audit/guo/index.html`

This campaign **supersedes** the first-screen layout frozen in
`HUMAN_AUDIT_HTML_FREEZE.md`. It does not supersede statuses, counts,
ZERO semantics, or `v0.3.0-alpha`.

`draft-v3` unchanged. Verifier unchanged. Tag not moved.
Not draft-v4. Not humanizer. Not five-paper HTML.

---

## Why the previous freeze is reopened (presentation only)

Cold-reader metrics passed, but the page still failed as a **product
surface**:

- Derivation map / Edge evidence were JS-assembled empty boxes
- Flagship teaching edge (D-117 → Eq. (5)) was a table row, not a card
- Eight-way status legend competed with the three denominators
- `NONZERO=0` could still be read as “the paper has no incorrect steps”
- Offline / no-JS floor was a warning, not a ledger

---

## First-screen contract

1. Kicker: **Evidence ledger** (not “Full derivation audit”).
2. Trust: two sentences. Hash, 0/155, 15+2 vs 17 live in the footer.
3. Three **equal** cells: inventoried equations / extracted relations /
   executable obligations. Coverage 100% only as inventory small type.
4. One stacked bar over 146 relations, **line style = evidence kind**
   (not traffic-light fill).
5. One default-open residual card: Eq. (D-117) → Eq. (5).
6. Swimlanes: Main + Appendix D open; other appendices collapsed with
   counts. Arrows are frozen edges, not proven implication. Ellipsis
   (`⋯`) is not an arrow.
7. Full RESULTS table in the HTML even if JS is off.

Visual success is whether a physicist can say, without YAML:

- the main-formula \(O(\Gamma)\) is not certified
- much Appendix D index algebra is exact zero
- D-117 → (5) is directly nonzero because
  \(f_n^{(4)}=2f_{0,n}^{(4)}\) is not yet applied

---

## What this presentation still is not

- a certificate
- a new experiment
- residual retuning
- assumption-graph IR in the engine
- an LLM paper reviewer
