# ssc-bench-v0.2-hard

New version. Does **not** overwrite `benchmark/` (ssc-bench-v0.1).
v0.1 test remains frozen and unused for Method v2 selection.

Emphasis: D2–D5 scientific compactification. Guo is **dev / case study**.

Hidden fields stay out of `proposer_view` (same policy as v0.1).
