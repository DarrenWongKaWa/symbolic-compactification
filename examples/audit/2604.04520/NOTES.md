# NOTES — arXiv:2604.04520

The first HTML pass listed 33 TeX `eq:` labels and extracted 12 edges.
The appendix is most of the paper: **82 numbered display lines** vs **12** in the Letter.

This update inventories every numbered `equation`/`align`/`gather` line (no `\nonumber`).
Appendix derivation steps are now edges, almost all orange:

- Green functions / Matsubara diagrams / tadpoles / bubbles: `look` (not compiled).
- Taylor in ω, clean-limit Γ expansions, O(Γ^n): `remainder` (finite terms do not prove O(·)).
- Gauge / static-A vanishing, and the i∂_kΔ/Δ cancel: Sign queue (not auto-green).

Still greened only: 2-channel unitarity |t_LR|²=|t_RL|² after A: S†S=SS†=I.

missing_declared_moves: not certified. Adjacent appendix lines without a cue are inventoried but not turned into fake edges.
