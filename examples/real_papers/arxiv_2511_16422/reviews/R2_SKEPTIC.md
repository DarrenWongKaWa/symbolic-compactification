# R2 — verification skeptic

**Verdict: CAVEATED**

All 18 `TABLE_VERIFIED` rows are integrity-bound engine ZERO on the displayed
residual strings. `D.gamma-asymptotic` and both IBP edges are correctly
excluded. No production verifier patch. Markdown cannot create ZERO.

Six of eighteen residuals are post-substitution forms
(`e21=-e12`, $\Omega^2=-\Omega^1$, $f'=2f_0'$, metric-velocity pair,
$\Omega$ definition). Unsubstituted cited lhs/rhs files are NONZERO for
those six. That is allowed v0.2 encoding and is weaker than “the two cited
equation files are identical.” `D.compact-nbar` is a convention-substituted
tautology: ZERO on the residual, not a proof of the factor of two.

Not BLOCKED: no forged ZERO, no remainder/IBP promotion.
