# Final Scientific Verdict — Scalable Verified Representation Invention

Track V only in this close. Track D was not opened for Guo Hermite because
5-branch confluence remains UNKNOWN (not a fair D test). Synthetic Hermite
frozen P2 outputs already mix ZERO/NONZERO with `df` vs `F'` (D, not V).

## 1. What was the Track-V bottleneck?

Giant `sympy.limit` / size-guard UNKNOWN on Guo kernels that still contain
spectator `h1*h2`. After exact AppliedUndef factoring, 2-member double-sum
confluence is a **series** identity on the local polygamma kernel.

## 2. How much UNKNOWN was eliminated?

Frozen Guo P2 `local_confluence`: **3 / 8 → ZERO**. 5 remain UNKNOWN
(5-branch or mixed-family claims). 0 NONZERO.

## 3. Did frozen Guo hypotheses become ZERO or NONZERO?

**ZERO** on the three 2-member pairs (G0004/G0005 and G0008/G0009, including
a later seed replica). **Not NONZERO.** Hermite-typed frozen hyps stay
unscored by this cascade (0 ZERO).

This is **V_GAIN** on the same P2 JSON. Not discovery gain.

## 4. Was Guo primarily D-, G-, C-, or V-bound?

**Two-branch confluence: was V, now certified.**
**Five-branch / Hermite-typed: still V/C** (not shown NONZERO).
P1's 11/11 ZERO used a specialized compiler; V2 generic limit could not;
Track V compositional cascade recovered the 2-member subset.

## 5. Could DeepSeek systematically discover first Newton DD?

Unchanged from `91a401b`: DEV 2/5, TEST 3/5 DD-OK. Not 5/5. Not Guo G2.

## 6. Could it discover repeated-node/Hermite DD?

Still **no HERMITE_OK**. Frozen toy Hermite proposals use independent `df`
vs `F'`. Guo Hermite types remain UNKNOWN at 5-branch scale.

## 7. Could it discover a nontrivial master object?

Unchanged: substitution-only masters; tautological bait 5/5 abstain.

## 8. Did specialist subagents help?

Track D specialists were **not** run. Track V specialists (factor,
confluence cascade) produced the V_GAIN.

## 9. Did SOL help or anchor representation invention?

Unchanged. SOL not retuned. T1 D frozen.

## 10. What did DeepSeek-v4-flash replicate?

Unchanged: Newton DD-OK 1/3 DEV.

## 11. What were AI_UNIQUE_SUCCESS cases?

Unchanged polygamma Newton seed. New: **verifier-unique** recovery of
frozen Guo 2-member confluence without Guo-specific identities.

## 12. What did symbolic baselines still dominate?

B9 still names `divided_difference` on the Newton control without `F`.
LGG still empty on polygamma DD.

## 13. What representation level was reached on Guo?

Still **G1** (local confluence), now with a **generic** V path for the
2-member subset. Not G2/G3.

## 14. Which gains were D/G/C/V?

| gain | |
|---|---|
| V_GAIN | 3 frozen Guo confluence UNKNOWN→ZERO after factor+series |
| C_GAIN | not claimed (same compiled members, new V path) |
| D_GAIN | none this track (no new LLM) |
| G_GAIN | none (members already G####) |

## 15. What remained HUMAN_REQUIRED?

None for 2-member confluence. 5-branch coalescence still UNKNOWN — not
escalated to a physics assumption; it is a remaining V/C problem.

## 16. Which claims survived?

Compositional verification can eliminate some Guo UNKNOWN without
Guo-specific ZERO rules. False ZERO = 0 on the generic suite.

## 17. Which claims were falsified?

“Generic V2 size-guard means the Guo confluence claims are unverifiable
in principle.” They are verifiable after spectator factoring.

“Track V would also certify Hermite on Guo.” It did not.

## 18. Strongest positive result

`lim_{ε(m)→ε(n)}` of the True-branch kernel = Eq(m,n) branch, after
cancelling `h1*h2`, ZERO via series, on frozen P2 G0004/G0005 (and G0008/G0009).

## 19. Strongest counterexample

5-member local_confluence / hermite_divided_difference claims on the
triple-sum Piecewise (ops 573) remain UNKNOWN. Naming Hermite ≠ G3.

## 20. Publication decision

**E** still. Story A (compositional verification) is now a **partial**
result, not a paper by itself. Story C is not supported.

## 21. Exact commits / tags / benchmark hashes

| | |
|---|---|
| Evidence P2 | `91a401b` |
| Track V freeze | `329c49c` |
| P1 / P2 run files | hashed in `FROZEN_INPUTS.json` (50 files, 81 hyps) |
| Tag intent | `scalable-confluence-verifier-v1` |

## 22. Recommended next scientific question

Can a **5-branch / repeated-node** confluence identity be certified by
the same compositional cascade (pairwise limits + Hermite recurrence)
without Guo-specific algebra — and only then, can a new proposer
systematically emit HERMITE_OK?
