# Track V closed — scalable-confluence-verifier-v1

Frozen verifier strategies, router thresholds, obligation map, generic
suite, and Guo rescore. No further Track-V method changes.

## Generic suite

`GENERIC_SUITE.md`: **false ZERO = 0**. Newton, repeated-node, F[x,x,y],
F[x,x,x], removable singularity, common-factor split: ZERO. Adversarial
sign/denom/factorial/order/limit/pole: not ZERO.

## Frozen Guo P2 rescore (same LLM outputs)

8 `local_confluence` hypotheses in the frozen P2 set.

| outcome | n |
|---:|---:|
| ZERO (2-member double-sum branch pairs) | **3** |
| NONZERO | 0 |
| UNKNOWN (5-branch / mixed-family claims) | 5 |

Gain: **V_GAIN** (factor AppliedUndef spectators + series confluence).
Not D_GAIN. Historical run JSON was not rewritten.

Hermite-typed frozen hyps: **0 ZERO** (not discharged by this cascade).

## Case

**V-D:** local two-branch confluence can be certified; repeated-node /
Hermite on Guo remains unresolved (still V/C at 5-branch scale).

## Track D

May begin for **synthetic** Hermite/master tasks using this frozen
verifier. Guo Hermite remains verifier-bound; new Guo LLM calls cannot
be scored as discovery failures until 5-branch confluence is ZERO or
NONZERO.
