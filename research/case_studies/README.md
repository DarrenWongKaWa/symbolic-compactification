# Case studies (human interpretation slots)

Traces are Method v2 DEV unless noted. Physical meaning is not invented
beyond declared context.

## 1. Response / Kubo-like — `D2-shared-denominator`

- Raw: two double sums with the same energy denominator, channels v and w.
- Proposal: merge limits; kernel = `(v v + w w)/(e(n)-e(m)+I g)`.
- Verdict: ZERO. Named `Kbody` expands to ZERO.
- **Human slot:** the shared retarded denominator is the spectral kernel;
  two vertex channels add in the numerator. Downstream: one contraction,
  not two diagrams. Not a new physical approximation.

## 2. Thermal — `D3-thermal-pair`

- Raw: `A ψ(zP) + A ψ(zM)`.
- Proposal: factor A; name `Phi = ψ(zP)+ψ(zM)`.
- Verdict: ZERO on the expanded form.
- **Human slot:** a weighted even thermal pair. Useful as a named object
  for later reflection identities **if** a human declares `zP+zM=1`.
  The cot collapse is **not** certified.

## 3. Piecewise / confluence — `D4-piecewise-generic-kernel`

- Raw: `Piecewise((K(x,y), x≠y), (K(x,x), True))`.
- Blank: `K(x,y)` claimed proven.
- Method v2: no promotion. Engine UNKNOWN for the drop.
- **Human slot:** coincidence-as-limit is a research hypothesis about K,
  not a theorem of the residual engine. Physicists may assume continuity;
  that is `HUMAN_REQUIRED`.

## 4. Indexed pairing — `D5-index-pair`

- Raw: `h1(n,m)V(n,m) + h1(m,n)V(m,n)`.
- v2: names `P` and `Pswap`; expansion ZERO (tautological orbit sum).
- **Human slot:** S2 orbit of a directed vertex weight. Packaging, not
  a new identity. Helps writing “object + permute”.

## 5. Guo σ_abc (negative / partial)

- 2026-08-21 skill: certified L2 only.
- 2026-08-27 isolated proposer: L3–L5 *types* without bytes/ZERO.
- **Human slot:** still no certified master thermal function or geometric
  generators. Report as partial/negative. Do not fake the PRB form.
