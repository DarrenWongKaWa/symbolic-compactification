# Physicist-utility rubric

Question (blinded):

> Which equivalent representation is more useful for continuing theoretical analysis?

Rate each pair on:

1. exposes repeated mathematical structure
2. exposes symmetry
3. reduces independent analytic objects
4. makes limiting behavior easier
5. makes scaling easier
6. makes geometric meaning clearer
7. separates physically distinct channels
8. improves derivability / reuse

Do **not** ask which formula looks prettier.

Record: preference, confidence, reason category.

**This snapshot has no human annotations. Do not fabricate them.**

Deterministic proxies (not a substitute for the rubric):

- `index_swap` invariance via the residual verifier
- `n_named_aux`
- `symmetry_exposed` / `kernel_exposed` flags

These proxies can be ZERO/NONZERO facts; they are not expert preference.
