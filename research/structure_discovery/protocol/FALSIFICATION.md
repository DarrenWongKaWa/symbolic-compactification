# Falsification protocol

UNKNOWN is never success. Numeric agreement is never ZERO.

| Claim | Falsified when | Survives when |
|---|---|---|
| C1 | held-out B9 type-hit ≤ B1 and ≤ B6 | B9 type-hit strictly higher, not whole-E aliases |
| C2 | no D3–D5 type gap vs direct E→E' | D3–D5 type-hit gap on frozen test |
| C3 | any forbidden reconstruction is ZERO | 0 unsafe merges, 0 false promotions |

Whole-program falsification of the research *direction* (not just a claim):

- If B9 cannot propose D2 kernels that observations make obvious, the
  observation layer is broken (engineering, not science).
- If B9 never exceeds CSE/CAS on anything but names, Story A (method) is
  unsupported.
- If negative items are routinely ZERO, the verifier is not protecting state.

Do not retune test gold after seeing results. A failed DEV intervention may
be revised **once**; then freeze.
