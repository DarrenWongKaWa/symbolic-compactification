# Reviewer A — AI-for-Science (hostile)

Does this materially improve scientific reasoning?

**Recommendation: Weak reject / invite to workshop after LLM+S3.**

The paper I would be asked to review claims an AI can surface kernels,
masters, and generators in theoretical-physics algebra under exact checks.
What was actually run is a deterministic observer. On toys it works. On
Guo it names `epsilon(n)` 175 times and never proposes the Piecewise
confluence the source is built around. Human interpretation slots are
empty. I cannot tell whether a condensed-matter theorist would change a
derivation.

What I would accept later: blinded physicist preference on 30 real
expressions, plus an LLM discoverer that proposes H the observer does not.
Keep the fail-closed gate. Drop any claim of beating CAS.
