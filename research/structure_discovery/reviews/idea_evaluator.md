# Idea-evaluator (structure discovery)

Evaluate Stories A/B/C independently. Do not force a survivor.

## Story A — Verified scientific structure discovery method

**Verdict: Reject for now (Revise after LLM evidence).**

Mechanism is real: typed H + constructor + fail-closed residual. The
*scientific imagination* clause is not evidenced. B9’s D3–D5 hits are
exactly the observation features (repeated srepr, swapped args, difference
quotients, identical Piecewise values). A PL reviewer will reduce this to
CSE + pattern match with a residual gate. That can be a workshop method
*if* an LLM discoverer adds types the observer misses (Guo Piecewise,
perturbation anti-unification). Currently the LLM cell is empty.

## Story B — Scientific structure discovery benchmark

**Verdict: Reject as D&B.**

33 author-constructed items, 12 test, no public corpus, no inter-annotator
protocol, Guo not in the bench. The *design* (positive+negative, leakage
boundary, D0–D6 taxonomy) is the valuable part. Scale is not.

## Story C — Scientific compactness / utility evaluation

**Verdict: Out of scope.** No human pairwise data. Deterministic downstream
probes are not expert preference. Do not write this paper from this snapshot.

## Overall

Keep the artifacts. Do not draft. Re-open Story A only with a non-deterministic
proposer that beats B9-deterministic on tasks the observer cannot solve
(Guo-scale Piecewise confluence, anti-unified masters).
