# ssc-assumption-complete-science-v0.1

Partitions: DEV / TEST / CHALLENGE. Guo is **not** in TEST.
DEV proposal is in `../ADMITTED.json` (`DEV_proposal`). This is **not**
a TEST freeze (`final/FREEZE_MANIFEST.json` does not exist yet).

Executable smoke tasks (B0) live in `dev_smoke/`. They are assumption-
complete identities with declared domains. They are not the full DEV
catalog.
