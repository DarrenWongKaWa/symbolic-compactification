# Problem statement — assumption-complete representation discovery

Independent line after the **sealed** Guo G3 route (`9fc3c8a`).
Not Remainder V2. Not Guo D2. Not assumption backfill.

## Frozen Guo authority (permanent here)

```
G0016 → G0013 = UNKNOWN LEVEL_B
```

Guo is only: historical DEV, assumption-insufficiency control,
fail-closed verifier demo, source-grounding case study.
It is **not** a headline positive result, prompt-tuning target,
or verifier-extension target.

## Central question

When the scientific problem **explicitly** contains enough mathematical
and analytic assumptions for exact verification, can an LLM/hybrid
system discover nontrivial representations that are grounded,
operational, and machine-certified beyond frozen symbolic baselines?

```
raw scientific expression
  → grounded structural proposal
  → Newton DD
  → repeated-node / Hermite
  → master analytic object
  → compact scientific representation
```

Separate **representation-discovery weakness** from
**under-specified mathematical domain**.

## Success object

```
H = (R, {A_i}, {O_i}, F)    with    A_i = O_i[F]
```

Terminology, ungrounded formulas, and finite series without remainder
conditions are not discovery. Remainder invariant: negatives ZERO +
C0 ZERO + remainder UNKNOWN ⇒ hop UNKNOWN, never ZERO.

No experiment before this contract freeze. No paper directory unless
publication A/B/C/D.
