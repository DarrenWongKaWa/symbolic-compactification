CONDITION: P0_RAW

Input: source expressions, explicit assumption contract, scientific
context, grounded source catalog. No structural-observation packets.
No extra interpretation.

Propose operational H = (R, {A_i}, {O_i}, F) that could explain
non-identical catalog members. Identify members by catalog IDs.
Define F when the representation uses a latent object. Give operators,
instance maps, reconstruction, and exact proof obligations.

PUBLIC_TASK_ID: T03
Do not use an external name for this task.

DECLARED SYMBOLS:
['y']
DECLARED FUNCTIONS:
['im', 'polygamma', 'tanh']
ASSUMPTION CONTRACT (use only these; do not add undeclared domains):
- y is a real variable
- 1/2 + I*y is not a nonpositive integer (so polygamma(0, ·) is defined)
- tanh has no poles on the real axis

SCIENTIFIC CONTEXT:
- Identity relating Im polygamma(0, 1/2 + I y) to tanh(pi y) (NIST DLMF 5.4.17).
- y real; y = 0 is allowed.
- Do not invent physical names.

SOURCE CATALOG — cite these source_node_id values only.
IDs are local to this task. Do not invent aliases.

G0001  kind=expr
  text: im(polygamma(0, Rational(1, 2) + I*y))
G0002  kind=expr
  text: (pi/2)*tanh(pi*y)
G0003  kind=expr
  text: im(polygamma(0, Rational(1, 2) + I*y)) - (pi/2)*tanh(pi*y)

SOURCE EXPRESSIONS / CURRENT:
im(polygamma(0, Rational(1, 2) + I*y)) - (pi/2)*tanh(pi*y)

Respond with JSON only.