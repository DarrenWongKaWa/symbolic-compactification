# Guo policy

Sealed. Source-assumption audit `9fc3c8a`.

```
G0016 → G0013 = UNKNOWN LEVEL_B
```

Allowed: historical DEV, assumption-insufficiency counterexample,
fail-closed verifier demonstration, source-grounding case study.

Forbidden in this line:

- prompt tuning on Guo
- verifier extension to rescue Guo
- inserting `beta>0`, `gamma>0`, or real `epsilon`
- reading “finite Gamma” as `gamma>0`
- PRB-gold assumption import
- Track-D Hermite discovery **on Guo**
- headline positive representation result from Guo
- Remainder V2

A future **human** may define a new Guo problem. Do not do that here.
