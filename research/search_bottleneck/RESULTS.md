# Bottleneck experiment results (hard DEV, seed 0)

Date: 2026-08-26
Model: Grok isolated subagents, one call, up to 3 candidates.
Not frozen test. Guo is a contaminated case study. 5-seed flagship
**not run** (cost). R6 skipped. Guo proposers did **not** receive the
22 kB formula (structure_summary + scientific_context only);
mismatch `guo_text=summary_not_full_bytes`.

R3-det (named transforms, no LLM) is recorded separately from R3-LLM.

## Search vs certification (do not collapse)

### D2 kernel merge (weighted sum; shared denominator)

All of R1, R3, R4, R5, and R3-det produced a **ZERO** candidate that
extracts the shared kernel. Isolated scientific search is **not** required
for this D2 class: the production transforms already do it.

Unexpanded names (`s(n)`, `Inner`, `K` as Function) verify as UNKNOWN
until substituted. That is a **representation packing** issue, not a
missing special-function identity.

R1 extra Lorentzian / R5 extra symmetrized G: UNKNOWN (extra scientific
claim, not a local linearity identity).

### D3 thermal pair

| Arm | Attempted | Certified closed form |
|---|---|---|
| R3 / R3-det | D1 factor | ZERO D1 |
| R4 | named `PsiThermal` | UNKNOWN (name not expanded) |
| R5 | named `Phi` **and** expanded equivalent | ZERO; naming is D3 search, residual is D1 |
| R1 | factor + name + `cot` reflection | factor ZERO; cot **parse fail** (`cot` not in whitelist); reflection would need `zP+zM=1` (undeclared) |

Scientific naming appears in R4/R5/R1, not in conservative R3. Certified
D-level does not rise above D1.

### D4 generic Piecewise kernel

Dropping to `K(x,y)`: engine `UNKNOWN` (`simplification_undecided`),
**not** NONZERO. R1 set `claimed_proven=true` (unsafe). R5 hypothesized
confluence honestly (`claimed_proven=false`) but `Limit` is not in the
parser. R3 **also** proposed `K(x,y)` despite the conservative skill.
R4 refused collapse (prose).

### D5 index pair

All arms described an S2 orbit / transpose-symmetrizer. Almost none
returned a closed engine-checkable expression. Search reached D5
*narrative*; certification stayed at tautology/UNKNOWN.

### Fermi Piecewise (DEV)

R1: replace off-diagonal by `-1/z`, `claimed_proven=true`. Engine:
**not ZERO** (UNKNOWN on declared symbols; previously NONZERO with
`nonzero=true` at `z=-1/2`). Blank-style recurrence is not an
unconditional identity here.

R5: named Θ, kept Piecewise, refused unconditional identity. No ZERO
progress. Safer search, same certified shallowness.

### Guo (case study, no full bytes this run)

R1: two named kernels + **drop Piecewise as limits**, `claimed_proven=true`.
Same *type* as 2026-08-21 blank arm. Not verified.

R5: shared vertex kernel, master thermal `F_master` with **keep
Piecewise until a limit identity is proved**, index-orbit / Cartesian
permutation. D3–D5 *hypothesis families* without gold names
`Phi_Gamma` / nine generators. `claimed_proven=false`. Not verified.

R3 historical (2026-08-21): certified L2 only.

## Matched-budget mismatches

- Seed 0 only.
- Guo without full expression (both R1 and R5).
- R3-det has 0 LLM calls; R3-LLM has 1.
- R2 is R1 text through the engine (no extra LLM).

## Direct answers to “≫”

On D2 certified level: **no material difference** among R1–R5.
On D3–D5 *attempted* structure: R5 ≥ R4 ≥ R1 > R3 (naming / confluence /
geometry talk).
On *certified* D-level above D2: **nobody**.
On unsafe claims: R1 ≫ others (`claimed_proven` on UNKNOWN/non-ZERO).

Syntactic Δops on D2 items is the same ZERO merge for all arms that
found it. Ops cannot decide this phase.
