# Frozen claims — abstraction invention v0.1

C1 — NON-IDENTICAL INVENTION  
A method that is not exact-srepr CSE proposes a parameterized template
\(F(\theta)\) whose instances recover related but non-identical members,
on items where frozen B9 invention-success is 0.

C2 — LLM ABSTRACTION (untested here)  
An LLM emitting machine-readable \(H=(T,\theta,\mathcal O,F)\) beats both
frozen B9 and LGG on Guo-scale families. **Blocked** this snapshot.

C3 — OBLIGATION SOUNDNESS  
Each instance map is adjudicated \(A_i - \mathcal O_i[F]=0\). UNKNOWN is
not success. Unrelated families are not certified.

Falsify C1 if M_lgg ≤ B9 on held-out positive invention.
Falsify C3 if a forbidden unrelated pair is certified ZERO.
