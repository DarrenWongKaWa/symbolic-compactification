# Owner: Subagent G — Guo DEV catalog (evaluation infrastructure)

Catalog: 4 Sums, 14 Piecewise branches, fingerprints, indices, energies,
special-function calls, G#### ids.

Proposer-visible modules are `catalog.py` and `proposer_view.py`.
They must not match `research.representation_invention.labels.FORBIDDEN_GOLD_PATTERNS`.

Evaluation queries live under `eval/` and are not shown to the proposer.
`COUNTS.md` is evaluation-only.

Do not decide the representation. Evaluation queries only.
