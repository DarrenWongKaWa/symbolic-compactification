# Publication decision

**E — PROMISING BUT MORE EVIDENCE NEEDED**

Not A/B/C. No `paper_representation_invention/` directory.

Why not C (Story C: LLM discovers DD/master beyond baselines):
- DD-OK is real but sparse (Newton 2/5 DEV seeds; polygamma 1/5).
- Frozen B9 already *names* `divided_difference` on the clean Newton control
  without an explicit latent \(F\). P2's gain is operational \(A_i=O_i[F]\),
  not the first use of the word.
- Guo P2 proposes local confluence / occasional Hermite type but the generic
  verifier returns UNKNOWN / COMPILE_FAILURE (size). P1's specialized
  confluence compiler remains the Guo ZERO authority (11/11). That is
  compiler-bound, not a new certified Guo DD.

Why not D only:
- Phase 5 generic Newton/Hermite/confluence stack is sound (false ZERO = 0).
- Grounded V2 contract works. Tautological-master bait: 5/5 abstain.
- One AI_UNIQUE seed: polygamma Newton with explicit \(F(t)=\psi(t)\),
  B9/LGG empty.

Revisit after: more DD-OK replication on held-out TEST, a generic Guo-scale
limit that is ZERO without Guo-specific rules, or a nontrivial master with
two operator kinds.
