# Adversarial self-audit (five attacks)

Not independent human reviewers. Written to record how the five protocol
attacks currently land.

## Reviewer 1 — AI-for-Science

Does this help scientific reasoning? **Not yet on Guo-scale physics.**
Certified Newton DD exists on synthetic and polygamma controls. Guo remains
P1 local confluence. Do not open a physicist survey.

## Reviewer 2 — PL / symbolic algebra

Is this known DD plus prompting? **Partly.** Constructors implement textbook
Newton/Hermite. B9 already *labels* `divided_difference` on the Newton
control without \(F\). P2's extra is grounded \(A_i=O_i[F]\) plus ZERO.
Newton/Hermite math is not novelty (`literature/NOVELTY.md`).

## Reviewer 3 — theoretical physicist

Does the representation expose real structure? **On toys, yes (Newton).**
On Guo, the model points at the right branch pairs but the generic verifier
cannot certify the polygamma confluence limit. No \(\Phi_\Gamma\).

## Reviewer 4 — benchmark skeptic

Were targets authored around the method? DEV Newton/Hermite tasks are
intentionally the method's object. TEST copies the same families with
relabeled expressions. Guo is a real expression, DEV-only. The tautological
and wrong-sign negatives are the anti-overfit checks; they behaved.

## Reviewer 5 — causal-experiment skeptic

Did multiple interventions change at once? P1 vs P2 changes the **output
contract** (V2 schema). SOL ranking was not retuned. After P2 calls, scoring
was fixed (full texts, size guard) — that is **compiler gain**, labeled as
such, and did **not** produce Guo ZERO. T1 D remains.

Decision: **E**.
