# Problem statement — source assumption audit

Not Remainder V2. Not Track D2. Not a hop-engine retune.

Frozen hop authority remains:

```
G0016 → G0013 = UNKNOWN LEVEL_B
```

Negatives \(t^{-6}\ldots t^{-1}\) are ZERO and \(c_0=G0013\). Remainder
is not certified because a pole-free neighborhood for

```
z0 = 1/2 + β(γ ± i(μ − ε))/(2π)
```

was not proved from **declared** assumptions.

## Question

Is pole exclusion already in the frozen Guo mathematical problem
(declared or derived), or is it a new restriction?

Three allowed closes only:

| close | meaning |
|---|---|
| **DECLARED** | source already states the needed domain; ingestion dropped it |
| **DERIVED** | frozen assumptions prove all 14 \(z_0\) avoid \(\mathbb Z_{\le 0}\) |
| **TRULY ADDITIONAL** | hop stays ASSUMPTION_REQUIRED / UNKNOWN LEVEL_B unless a human changes the problem definition |

Physical intuition (\(\beta=1/T>0\), broadening \(\Gamma>0\)) is **not**
a license to insert inequalities. Denominators in the generic formula
are **not** automatic domain declarations: \((\varepsilon(m)-\varepsilon(n))^{-3}\)
is the confluence pole the hop is about.

No LLM. No Guo identity table. Do not promote LEVEL C in this line.
