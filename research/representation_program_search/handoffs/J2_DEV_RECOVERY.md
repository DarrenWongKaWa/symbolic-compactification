# J2 machine-package recovery handoff

Branch: `work/rps-dev-recovery`

Implementation commit: `070c91d`

Prerequisite commits carried by this branch:

- `cd45dec` — expose the already-frozen COMPOSE operator through its legal
  search action;
- `9ba23df` — coordinator's `real:false` namespace contract-defect record
  (original coordinator commit `0a5d92e`).

## Delivered

- one strict M1-native `RPSCasePackageV1` artifact with exact source/member
  hashes, opaque proposer view, explicit executable outputs, canonical ids,
  full and primitive programs, and eight recorded exact ZERO sessions;
- a deterministic validator and machine/Markdown recovery audit;
- source, duplicate, leakage, CSE, depth, and grammar-ablation review;
- a fail-closed registry leaving R2, R3, R4/R5, and R6 missing;
- six recovery tests.

## Scientific disposition

Nothing is admissible. The M1-native R3 artifact is explicitly
`PACKAGING_GAP` / `INELIGIBLE`: its primary source is arbitrary complex, but
the frozen implementation interprets every `real:false` symbol as
mathematically non-real. Its eight ZERO receipts are preserved only as
hash-bound contract-defect evidence. They must not enter DEV, be promoted, or
be cited as program-search success.

The prioritized phi recurrence is also `PACKAGING_GAP`. Complex/nonzero symbols
do not parse, while a Piecewise removable-value representation loses its zero
stratum under the same `real:false` defect. No apparent ZERO from that collapsed
domain was recorded or retained.

R6 was left missing because all parser-feasible reductions of the available
matrix/block/integral masters independently collapsed to shallow scalar/CSE
objects. R2 was left missing rather than selecting a duplicate or grammar-bait
case.

## Verification

Focused suite:

```text
27 passed in 2.21s
```

Command:

```bash
PYTHONPATH=. /Users/kawawong/Projects/symbolic-compactification/.venv/bin/python \
  -m pytest -q tests/test_rps_dev_recovery.py tests/test_rps_program_ir.py
```

The validator result is `VALID_PACKAGING_GAP_EVIDENCE`, never
`ADMISSION_READY`.

Repository-wide suite:

```text
1746 passed, 3 failed in 188.53s
```

All three failures are the same pre-existing order-sensitive test-harness
defect: importing `research...packages.matrix_diffphys.validate` creates a
`__pycache__/` directory, after which
`tests/test_rps_matrix_diffphys_packages.py` mistakes that directory for a
case package and attempts to open package artifacts inside it. No recovery
package, compiler, verifier, or scientific assertion failed. After removing
only the generated bytecode cache and disabling bytecode writes, the affected
package tests plus both recovery/IR suites passed:

```text
34 passed in 2.50s
```

Command:

```bash
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. \
  /Users/kawawong/Projects/symbolic-compactification/.venv/bin/python \
  -m pytest -q tests/test_rps_matrix_diffphys_packages.py \
  tests/test_rps_dev_recovery.py tests/test_rps_program_ir.py
```
