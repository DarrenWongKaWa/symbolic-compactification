# Problem statement — verified representation program search

Parent authority: `research/assumption-complete-representation-v1` @ `0cdde49`.

This is a **new experiment**. It is not prompt-tuning, SOL v2, Guo D2,
verifier rescue, parser extension, or remainder certification.

## Frozen negative result (do not reinterpret)

Publication **F**: core AI representation advantage not supported.

Established:

1. Assumptions can be made complete.
2. Grounded schemas and exact verification work on CORE_COMPARABLE tasks.
3. Frozen symbolic operational baselines had limited representation coverage.
4. Free-form DeepSeek sometimes built an explicit R2 Newton-DD object on
   one shallow DEV resolvent cluster.
5. That R2 signal had prompt leakage, was cluster-local, and did not
   replicate on held-out TEST.
6. R3+ proposals did not certify.
7. SOL did not help and sometimes harmed.
8. Specialist prompts increased terminology/tautology, not certified depth.
9. Held-out operational advantage was zero.

Therefore the next hypothesis is **not** “a better prompt will invent
representations.”

## New hypothesis

Free-form generation is the wrong search mechanism.

Mathematical representation invention may require **explicit search**
over a structured space of latent objects, operators, node
multiplicities, compositions, and reconstruction programs.

## Central question

Can explicit representation-program search discover operational,
machine-certified abstractions beyond:

A. frozen symbolic baselines;

B. free-form LLM proposal;

and does an LLM materially improve that search when used **only** as a
heuristic?

Causal stack:

```
representation grammar
    +
search algorithm
    +
optional LLM heuristic
    +
exact verifier
```

Determine which component provides any gain. Do not optimize toward
proving that AI is useful.

## Success object

```
H = (F, {A_i}, {O_i}, {theta_i}, assumptions)
A_i = O_i[F; theta_i]
```

A representation is a **program**, not prose.

## Inherited invariants

```
G0016 → G0013 = UNKNOWN LEVEL_B
```

Guo is sealed. Remainder: negatives ZERO ∧ C0 ZERO ∧ remainder UNKNOWN
⇒ UNKNOWN, never ZERO. PACKAGING_GAP is not method superiority.
TYPE_ONLY is not operational representation.

## Old TEST

The previous held-out TEST is **HISTORICAL_DIAGNOSTIC** only. It is not
headline TEST for this line. Fresh scientific cases required.

No experimental result before this contract freeze. No paper directory
unless publication A/B/C/D.
