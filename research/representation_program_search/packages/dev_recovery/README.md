# M1-native package recovery

These packages are pre-DEV candidates only. They do not select a partition and
do not alter the independent finding that the preceding 13 packages had zero
admissible cases.

- `rps-candidate-j2-001`: an M1-native fixed two-by-two scalar-component
  lowering of a second matrix-function Fréchet derivative. The evaluator
  program and primitive-only ablation compile and produced eight exact ZERO
  receipts, but all receipts are ineligible because the source is arbitrary
  complex while the frozen `real:false` namespace is mathematically non-real.
  The package is retained as `PACKAGING_GAP`, never as a DEV candidate.
- The prioritized phi-function recurrence was attempted and rejected before
  package admission. `RECOVERY_GAPS.json` records the fail-closed namespace
  defect and exact source-document hash; no apparently-ZERO receipt from the
  collapsed domain was retained as evidence.

R2, R3, R4/R5, and R6 remain deliberately unfilled by this recovery lane. Available R2
objects were duplicate/grammar-bait risks, while parser-feasible reductions of
the available R6 matrix masters collapsed to shallow scalar/CSE objects.
