# Grounded-Proposer-v1

P0 = frozen DeepSeek-v1 typed proposer (aliases like `S1_True` allowed).
P1 = this line. Members must be IDs from a source catalog.

Locked:

- SOL v1 ranking / backends (`relations`)
- `deepseek-v4-pro`, thinking on, `reasoning_effort=high`
- scientific context
- token/time budgets

Changed: **hypothesis grounding requirement only.**

The catalog is not a SOL ranking change. It is the member universe
required by the contract. P0 never had to name source nodes.

## Outcomes (Guo, vs frozen P0 A2)

| | meaning |
|---|---|
| A | many groundable DD/confluence and ZERO → discovery was hidden by interface |
| B | groundable but mostly NONZERO → vocabulary without structure (fair D) |
| C | DD/confluence disappear → old G1 was linguistic guessing |
| D | grounded and well-typed but UNKNOWN → V bottleneck |

Do not retune SOL. Do not mutate frozen P0 files.
