# Grounded-Proposer-v1 vs frozen P0

Same SOL (`relations` packets, unranked-as-frozen), same `deepseek-v4-pro`
thinking/high, same Guo scientific context, same budgets.
Only the **member contract** changed: catalog `G####` IDs, no aliases.

Frozen P0 files were not mutated.

## Guo P1 A2 (3 seeds)

| seed | parse | types | dd/conf hyps | ZERO | NONZERO | UNKNOWN | G-fail |
|---:|---|---|---:|---:|---:|---:|---:|
| 0 | OK | symmetry + 4×confluent | 4 | 4 | 0 | 0 | 0 |
| 1 | OK | 3×confluent + 2×symmetry | 3 | 3 | 0 | 0 | 0 |
| 2 | OK | 4×confluent + 2×param | 4 | 4 | 0 | 0 | 0 |

**11 / 11** grounded confluence hypotheses certified ZERO.
0 PARSE_FAILURE on Guo. 0 NONZERO. 0 UNKNOWN.

Members are catalog branches (e.g. G0005 generic vs G0004 `Eq(m,n)` of
the \(h_1(b,n,m)h_2(a,c,m,n)\) sum; likewise the other double sum and
both triple-sum Piecewise kernels).

## Outcome vs the four cases

**Case A (local):** requiring grounding did **not** kill confluence.
It made the same local generic→diagonal limit systematically selectable
and ZERO on all three seeds.

This is **not** \(\Phi_\Gamma\), not a Newton DD of a thermal master,
not L4–L7. P1 did not emit `divided_difference` as a type; it emitted
`confluent_representation` with explicit node pairs.

Frozen P0 A2 on Guo: G1 vocabulary often present, but \(n_{\rm unique}=1\)
for DD/confluence under alias members. P1 shows the missing piece was
**G (grounding in the proposal)**, not absence of local confluence
discovery.

## CAL-G smoke

Parse OK, chose `parameterized_family` on `G0003`/`G0004` (bound, not
compiled as confluence). Contract works; toy did not use the confluence
type.

## Locked

T1 A2=4/5 D remains. SOL ranking unchanged.
P0 frozen outputs remain the historical control.
