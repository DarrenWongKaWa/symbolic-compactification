# DEV decision

**Keep Method v2 for the next evaluation cycle.** Success criteria in
`METHOD_DECISION.md` hold:

1. false promotion = 0
2. closed named candidates rose on 4/6 items
3. D2 ZERO did not regress
4. ≥2 items have extra certified steps after first ZERO (4 items)

The causal hypothesis is **partially** confirmed: the bottleneck was
stop-after-ZERO + unexpanded names, and the loop+expand fix packages
kernels/masters/generators as closed ZERO reconstructions.

It is **not** confirmed that v2 discovers new confluence or special-function
identities. D4 remains UNKNOWN (correct). Do not extend the verifier.

One packager revision (unique `Sum(Kbody, …)` text so `seen` does not
drop naming) was used, as allowed.

Next: freeze v2, build ssc-bench-v0.2-hard, run B4 vs v2. No second
architecture rewrite.
